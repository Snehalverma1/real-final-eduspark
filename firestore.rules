rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // FULL ACCESS: Allow read and write for all documents for rapid prototyping.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
